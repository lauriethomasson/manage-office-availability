# Copy this file to .env and fill in your key.
# Only used for the LLM fallback step, when a file doesn't match a known
# rule-based layout. Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=
